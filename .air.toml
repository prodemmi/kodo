[build]
cmd = "./build.sh"
bin = "kodo"
include_ext = ["go", "ts", "tsx", "css", "html"]

exclude_dir = [
    "tmp",
    "web/node_modules/",
    "web/dist/",
    "web/tmp/"
]

[build.color]
main = "blue"
watcher = "green"
build = "yellow"
runner = "magenta"
